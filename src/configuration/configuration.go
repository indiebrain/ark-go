// Package configuration provides a central namespace to house ark's
// configuration types independent of the interface used.
package configuration

// strcut Configuration provides ark's internal representation of user
// supplied data
type Configuration struct {
	AirportCode string
}
